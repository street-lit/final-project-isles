<%= form_for(@request) do |f| %>
  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
      <% @request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :requested_friend_id %><br>
    <%= f.number_field :requested_friend_id %>
  </div>
  <div class="field">
    <%= f.label :are_friends %><br>
    <%= f.check_box :are_friends %>
  </div>
  <div class="field">
    <%= f.label :did_confirm %><br>
    <%= f.check_box :did_confirm %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
