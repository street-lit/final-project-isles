<div class='container'>
  <div class='row'>
    <div class='m12 m4 l4'>
      <h3 class="center grey-text text-lighten-5 blogs">Your comment</h3>
      <div class="card">
        <div>
          <h5 class="big-padding">
            On the post, "<%= "#{@comment.post.title}" %>" in the blog, "<%= "#{@comment.blog.title}" %>" by <%= "#{@comment.blog.user.full_name}"%>, <%= @comment.user.full_name %> replied, "<%="#{ @comment.body }"%>"
          </h5>
        </div>
      </div>

      <div class="card big-padding">
        <div class="row">
          <div class="col s12 m2 l2">
            <%= link_to 'Back', @comment.post, class: "waves-effect waves-light btn light-green accent-4  left little-margin-top side-margin-left" %>
          </div>
          <div class="col s12 m2 l2">
            <%= link_to 'Edit Comment', edit_comment_path(:user_id => @comment.user, :blog_id => @comment.blog, :post_id => @comment.post), class: "waves-effect waves-light btn light-green accent-4 left little-margin-top side-margin-left" if @comment.user_id == @current_user.id %>
          </div>
          <div class="col s12 m4 offset-m4 l3 offset-l5">
            <%= link_to 'Delete Comment', @comment, method: :delete, data: { confirm: 'Are you sure?' }, class: "waves-effect waves-light btn deep-orange accent-3 big-margin-right left little-margin-top side-margin-left side-margin-right" if @comment.user_id == @current_user.id %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
